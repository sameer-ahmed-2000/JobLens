/**
 * Shared authentication fallback token for local demo mode.
 *
 * NOTE: This token matches the pre-seeded demo user ('default-user-id') initialized
 * by backend/app/services/seeder.py. In single-user demo environments, using this token
 * allows immediate out-of-the-box operation without requiring prior signup.
 */
export const DEFAULT_USER_TOKEN = 'default-user-token';
