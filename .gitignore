project.md
deployment.md
database.db
*.env
# Test artifact databases — committed by mistake, now gitignored.
# Run: git rm --cached backend/test_*.db
# to stop tracking existing copies without deleting them locally.
test_*.db
